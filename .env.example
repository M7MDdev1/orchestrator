PORT=3000
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=ebra
PUBLIC_BASE_URL=http://localhost:3000
PROVIDER_BASE_URL=https://provider.com
GLOBAL_CONCURRENCY=30

# Worker settings
BASE_RETRY_SECONDS=30
MAX_ATTEMPTS=3
STALE_INPROG_SECONDS=900

# Provider simulation toggle
# Set USE_SIMULATOR=true (default) to use the bundled SimulatedProvider for local testing.
# Set USE_SIMULATOR=false to use the real HTTP provider implementation.
USE_SIMULATOR=true

# Optional: callback webhook signing secret if your provider supports signing
# CALLBACK_SECRET=your-secret
